chrome = {
	storage : {
		local : {
			get : function (){ },
			set : function (){ }
		},
		sync : {
			get : function (){ },
			set : function (){ }
		},
		onChanged : {
			addListener : function (){ }
		}
	},
	webRequest : {
		onBeforeRequest : {
			addListener : function (){ }
		}
	}
};
